<h1><%= @warehouse.name %></h1>

<table>
  <tr>
    <th>Name</th>
    <th>Quantity</th>
  </tr>
  <% @warehouse.items.each do |item| %>
    <tr>
      <td><%= item.name %></td>
      <td><%= item.quantity %></td>
    </tr>
  <% end %>
</table>
<br>

<%= link_to 'Back', warehouses_path %>